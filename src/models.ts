export type Todos = {
  name: string | number
}

export type ApplianceForm = {
  name: string,
  count: number,
  consumption: number,
  runtime: number
}

export type ApplianceEstimate = {
  name: string,
  count: number,
  estimate: number
}

export type Pokemon = {
  name: string,
  url: string
}